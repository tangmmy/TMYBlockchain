# 1️⃣ Ignore `node_modules` (installed dependencies)
node_modules/

# 2️⃣ Ignore TypeScript build output (compiled JavaScript files)
dist/
build/

# 3️⃣ Ignore TypeScript cache files
*.tsbuildinfo

# 4️⃣ Ignore environment variables and sensitive files
.env
.env.local
.env.development
.env.production
.env.test

# 5️⃣ Ignore logs and debugging files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
turbo-debug.log*

# 6️⃣ Ignore IDE/editor-specific files
.vscode/
.idea/
.DS_Store
Thumbs.db

# 7️⃣ Ignore package-lock files (optional, only if using `pnpm` or `yarn`)
# npm automatically regenerates this, but some teams prefer to commit it
# Uncomment the next line *only* if you want to ignore `package-lock.json`
# package-lock.json
